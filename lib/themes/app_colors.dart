import 'package:flutter/material.dart';

class AppColors {
  // ── Brand ─────────────────────────────────────────────────────────────────
  static const primary = Color(0xFF2196F3);
  static const primaryDeep = Color(0xFF004AC6);
  static const secondary = Color(0xFF4F378A);

  // ── Neutrals ──────────────────────────────────────────────────────────────
  static const black = Color(0xFF000000);
  static const white = Color(0xFFFFFFFF);
  static const grey = Color(0xFF9E9E9E);
  static const verylightGrey = Color(0xFFF5F5F5);
  static const iosBg = Color(0xFFF2F2F7);

  static const lightGrey = Color(0xFFF5F5F5);
  static const darkGrey = Color(0xFF616161);
  static const greyBlue = Color(0xFF3D6FA3);

  // ── Status (semantic) ─────────────────────────────────────────────────────
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFB80000);
  static const warning = Color(0xFFFFC107);
  static const info = Color(0xFF2196F3);

  // ── UI shell ──────────────────────────────────────────────────────────────
  static const divider = Color(0xFFE0E0E0);
  static const disabled = Color(0xFFBDBDBD);

  // ── Surfaces & borders ────────────────────────────────────────────────────
  static const surface = Color(0xFFFFFFFF);
  static const bgCanvas = Color(0xFFF7F9FB);
  static const border = Color(0xFFE2E8F0);
  static const borderFaint = Color(0xFFF1F5F9);

  // ── Slate scale (text, muted) ─────────────────────────────────────────────
  static const slate50 = Color(0xFFF8FAFC);
  static const slate400 = Color(0xFF94A3B8);
  static const slate500 = Color(0xFF64748B);
  static const slate600 = Color(0xFF475569);
  static const slate700 = Color(0xFF334155);
  static const slate900 = Color(0xFF0F172A);

  // ── Amber scale (alerts, low-stock) ───────────────────────────────────────
  static const amber50 = Color(0xFFFFFBEB);
  static const amber200 = Color(0xFFFDE68A);
  static const amber700 = Color(0xFFB45309);
  static const amber900 = Color(0xFF78350F);
  static const amberStock = Color(0xFFD97706);

  // ── Stock status ──────────────────────────────────────────────────────────
  static const stockInBg = Color(0xFFF0FDF4);
  static const stockIn = Color(0xFF15803D);

  // ── Tints (category icons, chips, badges) ─────────────────────────────────
  static const tintBlueBg = Color(0xFFEFF6FF);
  static const tintGreyBg = Color(0xFFF3F4F6);
  static const tintViolet = Color(0xFF7C3AED);
  static const tintVioletBg = Color(0xFFF5F3FF);
  static const chipSelectedBg = Color(0xFFEEF2FF);
  static const chipPaleBg = Color(0xFFF1F6FB);
  static const chipPaleBorder = Color(0xFFE1ECFA);

  // ── Category thumbnail gradients (inventory mobile cards) ─────────────────
  static const gradMedStart = Color(0xFF003A9E);
  static const gradMedEnd = Color(0xFF2563EB);
  static const gradEquipStart = Color(0xFF0F2B22);
  static const gradConsumStart = Color(0xFF1A1A2E);
  static const gradConsumEnd = Color(0xFF16213E);
  static const gradDefaultStart = Color(0xFF134E4A);
  static const gradDefaultEnd = Color(0xFF0D9488);
}
